#!/usr/bin/env bash
#MISE description="Add a GPG collaborator who can decrypt this repo"
#USAGE flag "--gpg-key <fingerprint>" help="GPG key fingerprint" required=#true
#USAGE example "notes add-user --gpg-key ABCDEF0123456789" header="Add one collaborator"
set -euo pipefail

source "$MISE_CONFIG_ROOT/lib/common.sh"
require_git
require_initialized
require_rudi

cd "$TARGET_DIR"
rudi add-user "$usage_gpg_key"
